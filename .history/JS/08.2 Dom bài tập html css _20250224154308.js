const modal = document.querySelector(".modal");
// console.log(modal);
if (modal) {
    setTimeout(function () {
        modal.classList.add("is-show");
    }, 3000);
}
// => hiện bảng trong div modal sau 3 giây khi mình lập trình cho HT
